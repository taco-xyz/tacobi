import { FC } from "react";
import { CardInternalProps } from "@/components/card";

export const KPICard: FC<CardInternalProps> = ({
  title,
  description,
  children,
  sources,
  lastUpdated,
  loadingState,
}) => {
  return <div>KpiCard</div>;
};
