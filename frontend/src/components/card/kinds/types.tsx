/**
 * The kind of the card.
 * - `full` - A full card that displays all the data, including legend, sources,
 *   and other metadata.
 * - `kpi` - A KPI card that displays a single metric.
 */
export type CardKind = "full" | "kpi";
